[package]
name = "uv-bump"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/javidahmed64592/uv-bump"
license = "GPL-3.0-or-later"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
owo-colors = "4.3.0"

[profile.dist]
inherits = "release"
lto = "thin"
